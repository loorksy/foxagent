FOXAGENT_HTTP_PORT=18180
FOXAGENT_HOST_BIND=0.0.0.0
FOXAGENT_PUBLIC_ORIGIN=https://foxagent.lork.cloud

# Required after the auth hardening. Keep this file on the VPS only — never commit it.
APP_PASSWORD=
ADMIN_PASSWORD_HASH=
JWT_SECRET=
JWT_EXPIRE_MINUTES=720

# MetaApi MT5 bridge (optional) — token + account id from metaapi.cloud
METAAPI_TOKEN=
METAAPI_ACCOUNT_ID=
